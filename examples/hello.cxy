
func peek() : char => '\0'

struct Has {
    val: bool
}

func get(c: char): Has {
    if (c == '\n') {
        // do something
    }
    return Has{val: false}
}

func main(): !void {

    while get(peek()).val {
    }
}
